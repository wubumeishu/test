📜 《云水禅心 3.0》全栈深度开发白皮书
版本：v2.0（已整合架构评审修正）
最后更新：2026-05-09

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 架构决策总览（已锁定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

以下为经过架构评审后锁定的全局技术决策，所有 Phase 的实现必须遵守：

【数据库】     SQLite → PostgreSQL（Phase 1 第一步强制迁移，不可推迟）
【任务队列】   Celery+RabbitMQ → Redis + RQ（利用现有 Redis，零增量运维）
【AI 输出】    轮询方案 → SSE 流式打字机效果（用户体验对标 ChatGPT）
【账号体系】   以微信 OpenID 为系统唯一信任标识，手机号为可选补充，系统内主动绑定
【登出清理】   6步法核爆清理（见 Phase 1 详细说明）
【合盘模型】   单条记录 + 双视角 JSON + 懒失效标记（见 Phase 3 详细说明）
【社区策略】   放弃用户 UGC，改为官方 PGC 内容，规避合规风险
【海报合成】   禁用 html2canvas，必须使用 uni.createCanvasContext 或 wxml-to-canvas

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 Phase 1：账户底座与绝对防御体系
涵盖：串号残留、真实短信、密码与资料修改、数据库迁移
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【第零步：数据库迁移（必须最先执行）】

  迁移目标：SQLite → PostgreSQL
  原因：SQLite 在高并发写入（AI 精批队列、合盘计算）时存在全表锁，3.0 规模下不可接受。
  执行步骤：
    1. 新建 PostgreSQL 数据库实例
    2. 修改 .env 中的 DATABASE_URL 为 PostgreSQL 连接串
    3. 重新执行 alembic upgrade head，在 PostgreSQL 上初始化全部表结构
    4. 编写数据迁移脚本，将 zen_bazi.db 中的存量数据导入 PostgreSQL
    5. 验证数据完整性后，废弃 SQLite 文件

─────────────────────────────────────────
1. 数据库模型重构（FastAPI + SQLAlchemy）
─────────────────────────────────────────

User 表结构（完整版）：

  字段名            类型                    说明
  ──────────────────────────────────────────────────────
  id                Integer, PK
  wechat_openid     Varchar, Unique, Nullable   微信唯一标识，系统绝对信任
  phone             Varchar, Unique, Nullable   系统内主动绑定，不信任微信动态手机号
  hashed_password   Varchar, Nullable           passlib[bcrypt] 单向加密，禁止明文
  avatar_url        Varchar                     默认系统头像链接
  nickname          Varchar                     默认随机禅意昵称，如"观星人_952"
  is_vip            Boolean, default=False
  vip_expires_at    DateTime, Nullable
  created_at        DateTime
  updated_at        DateTime

账号合并策略（防串号核心逻辑）：

  场景 1：用户先微信静默登录（无手机号）
    → 正常创建账号，phone 字段为 NULL
    → 用户可在【设置-个人资料】主动绑定手机号

  场景 2：用户先短信登录，后点微信一键登录
    → 后端检查该微信 OpenID 绑定的手机号是否与已有账号的 phone 一致
    → 若一致：自动 merge，将 wechat_openid 写入已有账号，不新建记录
    → 若不一致：提示用户"该微信已绑定其他手机号，请确认"

  ⚠️ 关键原则：系统绝对不信任微信返回的动态手机号（getPhoneNumber 接口）。
     手机号的唯一来源是用户在本系统内主动填写并通过短信验证码验证的记录。

─────────────────────────────────────────
2. 认证路由（/api/auth）
─────────────────────────────────────────

  POST /login/wechat     接收前端 code，换取 OpenID，静默注册，签发 JWT Token
  POST /login/phone      接收手机号 + 短信验证码，登录或注册
  POST /login/password   账号密码登录（保留兼容）
  POST /send-sms-code    发送短信验证码（含防刷限流）
  PUT  /profile          更新用户资料（nickname、avatar_url、phone 绑定）

阿里云短信防刷机制：

  集成库：alibabacloud_dysmsapi20170525
  Redis 限流规则：
    - 同一手机号：SETEX phone_code_lock:{phone} 60 1（60秒冷却）
    - 同一手机号单日上限：INCR phone_code_daily:{phone}，超过 5 次拒绝
    - 同一 IP 单日上限：INCR ip_code_daily:{ip}，超过 10 次拒绝

─────────────────────────────────────────
3. 前端"6步法核爆清理"（消灭串号）
─────────────────────────────────────────

  触发条件：
    A. 用户主动点击【退出登录】
    B. utils/request.ts 拦截器收到 HTTP 401（Token 失效）

  执行顺序（严格按序，不可调换）：

    步骤 1：捕获 401 或用户触发退出
    步骤 2：触发 AbortController，取消所有进行中的网络请求
            同时清除所有轮询定时器（clearInterval / clearTimeout）
            ⚠️ 必须在此步处理，否则 SSE 长连接或轮询回调会在跳转后
               继续向已 reset 的 Store 写入数据，产生"幽灵数据"
    步骤 3：路由强制跳转回 Login 页（必须先切走页面，再清数据）
            ⚠️ 若先清数据再跳转，当前页面监听 Store 的组件会因
               数据突然变为 null 而报错白屏
    步骤 4：执行 Pinia 深度重置
            useArchiveStore().$reset()
            useBaziStore().$reset()
            useUserStore().$reset()
            （所有 Store 必须全部列出，不可遗漏）
    步骤 5：执行本地存储清理
            uni.clearStorageSync()
    步骤 6：展示提示"登录已过期，请重新登录"

  前端 request.ts 封装要求：
    - 每个请求实例必须携带 AbortController signal
    - 全局维护一个 requestMap: Map<string, AbortController>
    - 步骤 2 中遍历 requestMap，逐一调用 .abort()，然后清空 Map

─────────────────────────────────────────
4. 前端资料补全（微信真实信息获取）
─────────────────────────────────────────

  静默登录流：App.vue 的 onLaunch 中执行 uni.login()，无缝换取 Token 存入本地。

  资料补全页（设置-个人资料）：
    - 头像：使用 <button open-type="chooseAvatar"> 获取微信头像
    - 昵称：使用 <input type="nickname"> 获取微信昵称
    - 手机号：用户主动输入 + 短信验证码验证，调用 PUT /api/auth/profile

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 Phase 2：破冰体验与首页/侧边栏降噪
涵盖：首页空状态、左侧抽屉菜单、快速排盘、测算大厅归类
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

─────────────────────────────────────────
1. 首页"迷雾破冰"交互（Vue3 UI）
─────────────────────────────────────────

  逻辑判定：if (archives.length === 0)

  空状态 UI：
    - 不显示运势 0 分
    - 整个雷达图和运势区块被遮盖：
      backdrop-filter: blur(15px); background: rgba(0,0,0,0.6)
    - 中心放置"建立专属档案"按钮
    - 呼吸灯动效：
      @keyframes breathing {
        0%   { box-shadow: 0 0 10px rgba(255,0,0,0.2) }
        50%  { box-shadow: 0 0 25px rgba(255,0,0,0.6) }
        100% { box-shadow: 0 0 10px rgba(255,0,0,0.2) }
      }

─────────────────────────────────────────
2. 左侧抽屉（Drawer）与聚合面板
─────────────────────────────────────────

  结构：点击左上角图标，滑出左侧抽屉，占宽 75%，不全屏遮挡。

  数据接入：
    - 月相：根据农历日期计算当前月相（朔月/上弦/满月等），展示对应 SVG 图标
    - 运势指数：根据用户默认档案返回今日财富/事业/姻缘指数（星级或进度条）
    - 禅意语录：接入一言 API 或自建 JSON 库，每天随机一句

─────────────────────────────────────────
3. 测算大厅与菜单重构
─────────────────────────────────────────

  快速排盘入口：
    - "灵光一现·快速排盘"（不存入长期档案，算完即走）
    - 放置于底部 TabBar 正中间大按钮，或首页核心区

  大厅归类（van-tabs 或自定义 Tab）：
    - Tab 1：东方命理
    - Tab 2：西方占卜
    - Tab 3：实用工具

  历史记录：从右上角移至【我的】页面

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 Phase 3：玄学引擎的专业度深挖
涵盖：经纬度/子时、塔罗优化、八字大运流年、合盘
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

─────────────────────────────────────────
1. 顶级八字排盘引擎升级（Backend lunar-python）
─────────────────────────────────────────

  真太阳时与地图选址：
    - 前端集成腾讯/高德地图 SDK，新建档案时可点击"地图选址"
    - 获取精确 latitude 和 longitude 存入 archives 表
    - 后端用经纬度 + 出生时间计算真太阳时，再排盘
    - 表单增加【区分早晚子时】开关

  大运流年与五行解析（引擎新增返回字段）：
    - 大运：10年一运的干支与起运年龄
    - 流年：当前年份干支
    - 当前节气下五行状态：旺、相、休、囚、死

  前端渲染：
    - Result 页新增【大运流年】Tab
    - 用时间轴（Timeline）组件展示一生气运起伏

─────────────────────────────────────────
2. 西方神秘学：塔罗深度优化
─────────────────────────────────────────

  视觉优化：
    - 洗牌动画时间：5秒 → 2秒
    - 卡牌区左右渐隐：
      mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent)

  AI 隐私解析增强：
    - 允许用户输入自定义问题（如"我什么时候能复合？"）
    - 隐私脱敏：后端预处理 Prompt，将真实姓名、地名替换为"该用户"、"此地"，
      再发送给 DeepSeek

─────────────────────────────────────────
3. 重磅杀手锏：合盘功能（Match-making）
─────────────────────────────────────────

  前端交互：
    - 新建【合盘】专属页面，展示两个带"+"号的空槽
    - 用户点击后，从底部 Archive 抽屉勾选两个已有档案（或快速临时输入）
    - 结果页提供【互换视角】按钮，前端瞬间切换两套数据源，无需重新请求

  ── 数据库层设计 ──

  match_results 表结构：

    字段名              类型                说明
    ──────────────────────────────────────────────────────────
    id                  Integer, PK
    archive_id_1        Integer, FK         永远存储两个 ID 中较小的那个
    archive_id_2        Integer, FK         永远存储两个 ID 中较大的那个
    match_data          JSONB               包含双视角完整结果（见下方 JSON 规范）
    is_stale            Boolean, default=False  懒失效标记
    is_unlocked         Boolean, default=False  付费/广告解锁标记
    created_at          DateTime
    updated_at          DateTime

    唯一约束：UniqueConstraint('archive_id_1', 'archive_id_2')

  入库排序规则（防止 A+B 与 B+A 重复）：

    archive_id_1 = min(input_id_a, input_id_b)
    archive_id_2 = max(input_id_a, input_id_b)

  ── 算法计算层 ──

  命理学合盘具有不对称性（主客视角）：
    A 看 B 与 B 看 A 的十神关系完全不同。
    例：A 日干甲，B 日干己 → A 看 B 为正财，B 看 A 为正官。

  后端必须建立明确的 Base（主）和 Target（客）规则引擎，
  一次计算跑出两套完整结果，同时入库。

  ── match_data JSON 规范 ──

  {
    "perspective_A_to_B": {
      "base_archive_id": <archive_id_1的值>,
      "target_archive_id": <archive_id_2的值>,
      "interactions": [
        {"type": "天干五合", "detail": "A看B为正财，主..."},
        {"type": "地支六合", "detail": "..."}
      ],
      "ai_report": "DeepSeek 生成的深度关系解析全文"
    },
    "perspective_B_to_A": {
      "base_archive_id": <archive_id_2的值>,
      "target_archive_id": <archive_id_1的值>,
      "interactions": [
        {"type": "天干五合", "detail": "B看A为正官，主..."},
        {"type": "地支六合", "detail": "..."}
      ],
      "ai_report": "DeepSeek 生成的深度关系解析全文"
    }
  }

  ── 懒失效（is_stale）机制 ──

  触发条件：archives 表任意记录发生 UPDATE 时，
    通过 SQLAlchemy 事件监听（@event.listens_for(Archive, 'after_update')）
    或显式调用，将所有 archive_id_1 或 archive_id_2 包含该 ID 的
    match_results 记录标记为 is_stale = true。

  前端展示逻辑：
    - is_stale = false：正常展示报告
    - is_stale = true：在报告顶部显示 Banner：
      "提示：发现档案已发生变动，当前为历史推演记录，如需重新推演，请[点击重新推演]"
      用户主动点击才触发重新计算，不自动删除旧记录。

  ⚠️ 严禁使用硬删除方案：用户可能已为该报告付费（观看广告或 VIP），
     强制删除会引发客诉，且无法恢复。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 Phase 4：社区生态与留存拉新
涵盖：发现社区、历史折叠、网页端SEO、每日一签游戏化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

─────────────────────────────────────────
1. "发现"模块（PGC 内容策略，放弃 UGC）
─────────────────────────────────────────

  ⚠️ 重要决策：放弃用户自由发帖（UGC）。
  原因：国内 UGC 平台需要互联网信息服务许可证，且靠人工审核无法防止
       违规内容导致 App 被封禁下架。小团队无力承担此合规成本。

  改为 PGC 策略（官方产出内容）：
    - 官方命理干货文章（由运营人员发布）
    - 精选测算案例（脱敏后的真实案例展示）
    - 官方设定的心理小测试（MBTI、九型人格等）

  Web 端与 SEO：
    - 使用 Nuxt.js 或静态 HTML 部署发现页内容到 Web 端
    - 配置标准 <title>、<meta name="description"> 标签
    - 目标长尾词：「INFP性格解析」、「八字伤官见官怎么破」等

─────────────────────────────────────────
2. 历史记录精细化
─────────────────────────────────────────

  标签化：列表增加 Tag 组件（[八字精批] [MBTI专业版] [塔罗单牌]）

  防刷屏折叠：
    - 前端 computed 属性，开启"折叠重复记录"时
    - 对 historyList 按 archive_id 进行 groupBy 聚合
    - 相同人的测算只显示最新一条，点击展开全部

─────────────────────────────────────────
3. 每日一签抽卡（Gamification）
─────────────────────────────────────────

  玩法：每天第一次打开 App，全屏弹出翻卡动效。

  卡面生成：后端结合今日运势（如"水旺"）生成行动建议（"今日宜穿蓝，向北漫步"）。

  裂变海报合成：
    ⚠️ 禁止使用 html2canvas（微信小程序无 DOM 结构，直接报错）
    必须使用：uni.createCanvasContext 原生 Canvas API
    或接入：微信官方 wxml-to-canvas 库
    合成内容：签文 + 用户专属邀请参数的微信小程序码
    输出：一键保存到相册，分享朋友圈

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏁 Phase 5：商业闭环与高并发护城河
涵盖：后台优化、AI异步队列、变现闭环
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

─────────────────────────────────────────
1. AI 深度解析：Redis + RQ + SSE 方案
─────────────────────────────────────────

  ⚠️ 已废弃方案：
    - Celery + RabbitMQ（运维过重，引入新中间件）
    - FastAPI BackgroundTasks（进程内任务，服务重启或高并发时任务全部丢失）

  最终方案：Redis + RQ + SSE 流式输出

  选型理由：
    - Redis 项目已有（用于短信限流），零增量运维
    - RQ（Python-RQ）极轻量，配置几行代码
    - SSE 实现打字机效果，用户感知"实时生成"，不觉得慢

  后端架构：

    1. 用户发起 AI 精批请求
    2. FastAPI 将任务推入 RQ 队列，立即返回 task_id
    3. RQ Worker（独立进程）从队列取任务，请求 DeepSeek
    4. DeepSeek 返回流式数据，Worker 将 chunk 逐段写入 Redis Stream
    5. 前端通过 SSE 连接 /api/task/stream/{task_id}，实时接收 chunk

  前端体验：
    - 获取 task_id 后，建立 SSE 连接
    - 界面展示"太极八卦阵加载动画"
    - 文案轮播："正在沟通宇宙能量..." → "正在生成深度报告..."
    - 内容逐字打印，直到收到 [DONE] 信号

  ⚠️ 运维清单（必须配置）：
    - RQ Worker 必须作为独立进程运行
    - 在 systemd 或 supervisor 中单独配置 rq worker 服务
    - 示例 supervisor 配置：
      [program:rq_worker]
      command=rq worker --with-scheduler
      directory=/path/to/project
      autostart=true
      autorestart=true

─────────────────────────────────────────
2. 三级变现漏斗（Monetization Funnel）
─────────────────────────────────────────

  免费层：四柱八字排盘图表直接展示。

  广告解锁层：
    - AI 万字深度报告被"毛玻璃 + 锁头"遮盖
    - 接入微信小程序/App 激励视频广告（uni.createRewardedVideoAd）
    - onClose 监听 isEnded: true 后，前端向后端发送解锁请求
    - 后端将该条记录的 is_unlocked 标为 true（合盘记录同理）

  VIP 订阅层：
    - 接入微信支付/支付宝支付
    - subscriptions 表记录用户 VIP 到期时间
    - VIP 用户绕过所有广告检测
    - App 内专属金色/深朱砂色尊贵标识

─────────────────────────────────────────
3. 运营后台（Admin CMS）
─────────────────────────────────────────

  技术选型：Vue3 + Element Plus（独立 Web 后台）

  核心功能：
    - 封禁违规用户
    - 发布/管理【发现】模块的 PGC 内容
    - 查看每日广告收益与新增注册折线图
    - 查看 RQ 任务队列状态（成功/失败/积压数量）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 执行优先级（推荐顺序）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  P0（立即）  Phase 1 - 数据库迁移至 PostgreSQL
  P0（立即）  Phase 5 - Redis+RQ+SSE 异步队列（解决现有 AI 超时问题）
  P1          Phase 1 - 账号体系重构（串号清理、账号合并）
  P2          Phase 2 - 首页破冰体验
  P3          Phase 3 - 八字引擎升级 + 合盘功能
  P4          Phase 5 - 变现漏斗（广告 + VIP）
  P5（最后）  Phase 4 - 发现模块 PGC 内容 + SEO

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
