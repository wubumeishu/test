# ✅ PostgreSQL 数据库配置成功！

## 🎉 配置完成

恭喜！你的 PostgreSQL 数据库已经成功配置并连接到 FastAPI 服务。

---

## 📊 配置信息

### PostgreSQL 配置
- **版本：** PostgreSQL 18.3
- **安装路径：** `G:\Program Files\PostgreSQL\18\`
- **端口：** 5433（非默认端口）
- **数据库：** zen_bazi
- **用户：** postgres
- **密码：** 123456
- **监听地址：** * (允许所有连接)

### FastAPI 服务
- **地址：** http://127.0.0.1:9000
- **状态：** ✅ 运行中
- **数据库连接：** ✅ 已连接

### 已创建的表
- **users** 表
  - id (主键，自增)
  - name (varchar(100), 非空)
  - email (varchar(255), 非空, 唯一)
  - created_at (timestamp, 自动)
  - updated_at (timestamp, 自动)

---

## 🔗 访问地址

- **API 文档（Swagger）：** http://127.0.0.1:9000/docs
- **健康检查：** http://127.0.0.1:9000/api/health
- **根路径：** http://127.0.0.1:9000/

---

## 📝 .env 配置

当前 `.env` 文件配置：

```env
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5433/zen_bazi
```

---

## 🚀 启动服务

```bash
cd bazi-admin
uvicorn main:app --host 127.0.0.1 --port 9000 --reload
```

---

## 🔍 数据库管理

### 使用 psql 命令行

```bash
# 连接数据库
psql -U postgres -d zen_bazi -p 5433

# 查看所有表
\dt

# 查看表结构
\d users

# 查询数据
SELECT * FROM users;

# 退出
\q
```

### 使用 pgAdmin

1. 打开 pgAdmin
2. 添加服务器：
   - 主机：localhost
   - 端口：5433
   - 数据库：zen_bazi
   - 用户：postgres
   - 密码：123456

---

## 📚 下一步开发

### 1. 创建新的数据模型

在 `src/models/` 目录下创建新的模型文件：

```python
# src/models/archive.py
from sqlalchemy import Column, Integer, String, Boolean
from src.models.base import Base, TimestampMixin

class Archive(Base, TimestampMixin):
    """档案表"""
    __tablename__ = "archives"
    
    id = Column(Integer, primary_key=True)
    name = Column(String(50), nullable=False)
    gender = Column(Integer, nullable=False)  # 1=男, 0=女
    birth_date = Column(String(20), nullable=False)
    birth_time = Column(String(20), nullable=False)
    relation = Column(String(20))
    is_default = Column(Boolean, default=False)
```

然后在 `src/models/__init__.py` 中导入：

```python
from src.models.archive import Archive

__all__ = ["Base", "User", "Archive"]
```

重启服务，表会自动创建。

### 2. 添加 CRUD 接口

在 `main.py` 中添加接口：

```python
from fastapi import Depends
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from src.database import get_db
from src.models.user import User

@app.get("/users")
async def get_users(db: AsyncSession = Depends(get_db)):
    """获取所有用户"""
    result = await db.execute(select(User))
    users = result.scalars().all()
    return users

@app.post("/users")
async def create_user(name: str, email: str, db: AsyncSession = Depends(get_db)):
    """创建用户"""
    user = User(name=name, email=email)
    db.add(user)
    await db.commit()
    await db.refresh(user)
    return user
```

### 3. 前后端联调

前端通过以下地址访问后端：

```typescript
// my-bazi-app/src/utils/request.ts
const BASE_URL = 'http://127.0.0.1:9000'

// 示例：获取用户列表
const response = await uni.request({
  url: `${BASE_URL}/users`,
  method: 'GET'
})
```

---

## 🔧 常用命令

### 查看数据库信息

```bash
# 列出所有数据库
psql -U postgres -p 5433 -c "\l"

# 查看当前数据库大小
psql -U postgres -d zen_bazi -p 5433 -c "SELECT pg_size_pretty(pg_database_size('zen_bazi'));"

# 查看表数据量
psql -U postgres -d zen_bazi -p 5433 -c "SELECT COUNT(*) FROM users;"
```

### 备份和恢复

```bash
# 备份数据库
pg_dump -U postgres -p 5433 zen_bazi > backup.sql

# 恢复数据库
psql -U postgres -p 5433 zen_bazi < backup.sql
```

---

## ⚠️ 注意事项

1. **密码安全：** 不要将 `.env` 文件提交到 Git
2. **端口冲突：** 确保 5433 和 9000 端口未被占用
3. **生产环境：** 需要额外配置连接池、SSL、备份策略等
4. **数据迁移：** 使用 Alembic 进行数据库版本管理

---

## 📖 相关文档

- **FastAPI 文档：** https://fastapi.tiangolo.com/
- **SQLAlchemy 文档：** https://docs.sqlalchemy.org/
- **PostgreSQL 文档：** https://www.postgresql.org/docs/

---

## 🎊 总结

你已经成功完成：

✅ 找到 PostgreSQL 安装路径（G:\Program Files\PostgreSQL\18\）  
✅ 确认端口配置（5433）  
✅ 创建 zen_bazi 数据库  
✅ 配置 FastAPI 连接  
✅ 创建 users 表  
✅ 启动服务并验证连接  

现在可以开始开发业务功能了！🚀
